# Database

## `migrations/0001_initial_schema.sql`

Esquema base extraído de la entrega existente, sin el bloque de datos ficticios.

## `reference/`

Consultas y pruebas SQL originales como referencia del diseño recibido.

## Importante

El archivo `usuarios_contrasenas_demo.csv` del ZIP original no se incluye en este starter porque contiene contraseñas demo en texto legible.

Los futuros seeds deben estar limitados a local/test y nunca reutilizarse en producción.
